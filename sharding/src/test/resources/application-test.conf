akka {
  actor {
    provider = cluster

    serialization-bindings {
      "example.sharding.CborSerializable" = jackson-cbor
    }
  }

  cluster {
    seed-nodes = [
      "akka://ContainerSpec@127.0.0.1:25520"]
    downing-provider-class = "akka.cluster.sbr.SplitBrainResolverProvider"
  }
}